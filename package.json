{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "lerna run dev --stream --parallel",
    "build": "lerna run build --stream"
  },
  "devDependencies": {
    "lerna": "^3.4.0"
  }
}
